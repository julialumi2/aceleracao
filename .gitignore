node_modules
dist
.env
.env.local
supabase-selfhost/.env

db-tooling/generated/
.claude/settings.local.json

# Docs de referência que o `prisma init` gera pra assistentes de IA —
# não fazem parte do app, cada dev pode gerar de novo com `npx prisma init`.
.agents/
.claude/skills/
.windsurf/
skills-lock.json
